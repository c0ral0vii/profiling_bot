services:
  profiling_bot:
    build: .
    container_name: profiling_bot
    env_file: .env
    environment:
      - PADDLE_PDX_DISABLE_MODEL_SOURCE_CHECK=True
    command: ./scripts/entrypoint-bot.sh
    restart: unless-stopped
    volumes:
      - .:/app
    ports:
      - 127.0.0.1:8001:8000
